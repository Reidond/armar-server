# Python / uv
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Environment
.env
.env.*

# armar runtime artifacts (per-deployment; server.toml may hold secrets, data/ is large)
/server.toml
/armar.lock
/data/

# OS / editor
.DS_Store

# Ephemeral binaries downloaded by download-actionlint.bash (CI/agents).
# Prefer: uv run --group lint actionlint
/actionlint

# Generated at Flatpak build time from the SVG source icon.
/flatpak/io.github.Reidond.ArmarManager.png
